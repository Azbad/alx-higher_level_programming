#!/usr/bin/python3
def uppercase(str):
    for i in str:
        temp = iterator
        if ord(temp) >= 97 and ord(temp) <= 122:
            temp = chr(ord(temp) - 32)
        print("{}".format(temp), end'')
        print()
